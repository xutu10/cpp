#include<iostream>
#include<vector>
using namespace std;

typedef vector<int> INT_VECTOR;

int main(int argc, char *argv[])
{
    INT_VECTOR v;
	v.push_back(1);
	v.push_back(2);
	v.push_back(3);

	int i;
	for(i =0; i<v.size();i++)
		cout<<v[i]<<" ";
	cout<<endl;
	
    return 0;
}
